import type { Client, ClientOptions } from 'discord.js';

export type DJSClient = Client;

export type DJSClientOptions = ClientOptions;

/**
 * Links:
 * - {@link !Guild}
 * - {@link !Message}
 * - {@link !hideLinkEmbed}
 * - {@link !User | custom}
 */
export const comment = true;
